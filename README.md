# 幸運轉盤

單一 HTML 檔的抽獎轉盤，沒有任何相依套件，直接用瀏覽器開 `index.html` 就能用。

- 自訂獎項與權重，權重越大越容易抽中，填 0 就不會出現在轉盤上
- 標題和說明直接點著改
- 按中間的 GO、「開始抽獎」或空白鍵都能轉
- 10 種配色風格可以切換
- 抽中後可選擇從轉盤移除（不重複中獎）
- 轉動音效、彩帶、抽獎紀錄；設定與紀錄存在瀏覽器 localStorage
- 有圖示與 manifest，手機可以加到主畫面
